version: "3.9"

services:
  redis:
    image: redis:7
    container_name: streaming-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "no"]
    restart: unless-stopped
